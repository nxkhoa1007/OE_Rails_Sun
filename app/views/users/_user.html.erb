<li>
  <%= gravatar_for user, size: Settings.gravatar_size %>
  <%= link_to user.name, user %>
  <% if check_user_role? user %>
    | <%= link_to t("destroy"), user, data: { "turbo-method": :delete, turbo_confirm: "You sure?" } %>
  <% end %>
</li>
